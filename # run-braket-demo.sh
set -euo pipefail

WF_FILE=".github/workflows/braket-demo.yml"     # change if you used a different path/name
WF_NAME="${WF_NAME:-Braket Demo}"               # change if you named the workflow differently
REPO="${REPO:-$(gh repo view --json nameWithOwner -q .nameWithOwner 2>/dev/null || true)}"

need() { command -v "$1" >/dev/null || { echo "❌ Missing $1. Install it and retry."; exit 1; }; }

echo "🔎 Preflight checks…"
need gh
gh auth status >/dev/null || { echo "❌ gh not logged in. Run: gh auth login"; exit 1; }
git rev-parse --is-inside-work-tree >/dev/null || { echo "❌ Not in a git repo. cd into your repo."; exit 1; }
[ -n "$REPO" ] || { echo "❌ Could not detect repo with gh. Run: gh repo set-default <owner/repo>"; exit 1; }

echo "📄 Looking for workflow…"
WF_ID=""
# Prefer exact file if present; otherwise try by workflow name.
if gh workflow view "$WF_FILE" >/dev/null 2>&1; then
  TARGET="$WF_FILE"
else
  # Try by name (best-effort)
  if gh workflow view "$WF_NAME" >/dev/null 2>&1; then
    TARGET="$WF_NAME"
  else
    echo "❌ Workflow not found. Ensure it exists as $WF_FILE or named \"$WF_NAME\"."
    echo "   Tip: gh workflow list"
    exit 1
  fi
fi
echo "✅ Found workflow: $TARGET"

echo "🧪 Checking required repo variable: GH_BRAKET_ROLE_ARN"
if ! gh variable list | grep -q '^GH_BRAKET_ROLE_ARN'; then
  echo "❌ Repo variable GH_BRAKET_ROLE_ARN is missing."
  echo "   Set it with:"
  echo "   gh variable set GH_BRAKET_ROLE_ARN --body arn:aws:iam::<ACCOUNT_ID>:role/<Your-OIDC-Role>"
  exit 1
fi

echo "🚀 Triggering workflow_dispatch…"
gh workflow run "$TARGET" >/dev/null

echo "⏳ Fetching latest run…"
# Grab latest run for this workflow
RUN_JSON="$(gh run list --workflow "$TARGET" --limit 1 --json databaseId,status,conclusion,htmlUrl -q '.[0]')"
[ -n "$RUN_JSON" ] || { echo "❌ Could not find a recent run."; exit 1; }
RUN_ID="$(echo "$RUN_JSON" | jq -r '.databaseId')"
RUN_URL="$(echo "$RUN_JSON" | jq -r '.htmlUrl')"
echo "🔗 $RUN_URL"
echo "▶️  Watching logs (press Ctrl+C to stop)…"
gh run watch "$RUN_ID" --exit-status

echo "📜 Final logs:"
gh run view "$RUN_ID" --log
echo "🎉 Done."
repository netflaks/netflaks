# run-braket-demo.sh
set -euo pipefail

WF_FILE=".github/workflows/braket-demo.yml"     # change if you used a different path/name
WF_NAME="${WF_NAME:-Braket Demo}"               # change if you named the workflow differently
REPO="${REPO:-$(gh repo view --json nameWithOwner -q .nameWithOwner 2>/dev/null || true)}"

need() { command -v "$1" >/dev/null || { echo "❌ Missing $1. Install it and retry."; exit 1; }; }

echo "🔎 Preflight checks…"
need gh
gh auth status >/dev/null || { echo "❌ gh not logged in. Run: gh auth login"; exit 1; }
git rev-parse --is-inside-work-tree >/dev/null || { echo "❌ Not in a git repo. cd into your repo."; exit 1; }
[ -n "$REPO" ] || { echo "❌ Could not detect repo with gh. Run: gh repo set-default <owner/repo>"; exit 1; }

echo "📄 Looking for workflow…"
WF_ID=""
# Prefer exact file if present; otherwise try by workflow name.
if gh workflow view "$WF_FILE" >/dev/null 2>&1; then
  TARGET="$WF_FILE"
else
  # Try by name (best-effort)
  if gh workflow view "$WF_NAME" >/dev/null 2>&1; then
    TARGET="$WF_NAME"
  else
    echo "❌ Workflow not found. Ensure it exists as $WF_FILE or named \"$WF_NAME\"."
    echo "   Tip: gh workflow list"
    exit 1
  fi
fi
echo "✅ Found workflow: $TARGET"

echo "🧪 Checking required repo variable: GH_BRAKET_ROLE_ARN"
if ! gh variable list | grep -q '^GH_BRAKET_ROLE_ARN'; then
  echo "❌ Repo variable GH_BRAKET_ROLE_ARN is missing."
  echo "   Set it with:"
  echo "   gh variable set GH_BRAKET_ROLE_ARN --body arn:aws:iam::<ACCOUNT_ID>:role/<Your-OIDC-Role>"
  exit 1
fi

echo "🚀 Triggering workflow_dispatch…"
gh workflow run "$TARGET" >/dev/null

echo "⏳ Fetching latest run…"
# Grab latest run for this workflow
RUN_JSON="$(gh run list --workflow "$TARGET" --limit 1 --json databaseId,status,conclusion,htmlUrl -q '.[0]')"
[ -n "$RUN_JSON" ] || { echo "❌ Could not find a recent run."; exit 1; }
RUN_ID="$(echo "$RUN_JSON" | jq -r '.databaseId')"
RUN_URL="$(echo "$RUN_JSON" | jq -r '.htmlUrl')"
echo "🔗 $RUN_URL"
echo "▶️  Watching logs (press Ctrl+C to stop)…"
gh run watch "$RUN_ID" --exit-status

echo "📜 Final logs:"
gh run view "$RUN_ID" --log
echo "🎉 Done."